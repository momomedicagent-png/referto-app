services:
  - type: web
    name: assistente-documenti-salute
    env: docker
    plan: free
    region: frankfurt  # puoi cambiare a us-east o oregon se preferisci
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GOOGLE_API_KEY
        sync: false   # la chiave la imposti da Dashboard di Render
